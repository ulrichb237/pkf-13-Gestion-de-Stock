export const environment = {
  production: true,
  // URL publique du backend en production. A ajuster au deploiement :
  // mettre l'URL reelle (ex : 'https://api.mondomaine.com') ou une chaine vide
  // si l'API est servie depuis le meme domaine que le frontend.
  apiUrl: 'http://localhost:8081'
};
